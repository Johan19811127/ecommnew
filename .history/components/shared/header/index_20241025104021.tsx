import { Button } from '@/components/ui/button'
import { APP_NAME } from '@/lib/constants'
import Menu from './menu'

const Header = async () => {
  return (
const Header = async () => {
            {APP_NAME}
          </Link>
        </div>
        <Menu />
      </div>
    </header>
  )